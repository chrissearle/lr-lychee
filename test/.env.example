# Copy to test/.env before first `docker compose up`.
# Lychee 7.7+ refuses to start without an APP_KEY.
#
#   echo "APP_KEY=base64:$(openssl rand -base64 32)" > test/.env
#
APP_KEY=base64:REPLACE_ME
